#include <stdio.h>

#define N 5

/**
 * - If rows has even index (e.g. 0, 2, 4...), then sort by ascending order.
 * - If rows has odd index (e.g. 1, 3, 5...), then sort by descending order.
 * @param numRows
 */
void crossSort(mat[][N], int numRows){

}

int main() { return 0; }